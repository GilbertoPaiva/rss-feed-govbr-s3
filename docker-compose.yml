version: '3'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      AWS_ACCESS_KEY_ID: "sua-chave"
      AWS_SECRET_ACCESS_KEY: "sua-secreta"
      AWS_REGION: "us-east-1"